import 'package:flutter/widgets.dart';

class AddTaskDesktopView extends StatelessWidget {
  const AddTaskDesktopView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
